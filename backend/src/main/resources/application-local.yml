cors:
  allowed-origins: http://127.0.0.1:3000, http://localhost:3000
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password:
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: create
    defer-datasource-initialization: true

recommend-api: http://localhost:5000